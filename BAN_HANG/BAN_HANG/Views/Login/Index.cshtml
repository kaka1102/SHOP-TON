
<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>INSPINIA | Login</title>
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/font-awesome/css/font-awesome.css" rel="stylesheet" />
    <link href="~/css/animate.css" rel="stylesheet" />
    <link href="~/css/style.css" rel="stylesheet" />
    <script src="~/Scripts/Ultils.js"></script>
</head>

<body class="gray-bg">

    <div class="middle-box text-center loginscreen animated fadeInDown">
        <div>
            <div>

                <h1 class="logo-name">IN+</h1>

            </div>
            <h3>Welcome to IN+</h3>
            <p>
                Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
                <!--Continually expanded and constantly improved Inspinia Admin Them (IN+)-->
            </p>
            <p>Login in. To see it in action.</p>
            <form class="m-t" role="form" id="frm-login">
                <div class="form-group">
                    <input id="username" style="color: black" type="text" class="form-control" placeholder="Tên đăng nhập">
                </div>
                <div class="form-group">
                    <input id="password" style="color: black" type="password" class="form-control" placeholder="Mật khẩu">
                </div>
                <button id="btn-login" type="button" class="btn btn-primary block full-width m-b">Đăng nhập</button>

                <a>
                    <small id="message-result" style="color: red;">

                    </small>
                </a>
            </form>
            <p class="m-t"> <small>Inspinia we app framework base on Bootstrap 3 &copy; 2014</small> </p>
        </div>
    </div>


<script src="~/js/jquery-3.1.1.min.js"></script>
<script src="~/js/bootstrap.min.js"></script>


<script type="text/javascript">
    $(function () {
        $(document).keypress(function(e) {
            if (e.which === 13) {
                LoginAction();
            }
        });
        BindButtonLoginClick();
    });

    function BindButtonLoginClick() {
        $("#btn-login").click(function() {
            LoginAction();
        });
    }

    function LoginAction() {
        var username = $("#username").val();
        var password = $("#password").val();
        var data = {
            username: username,
            password: password
        }
        CallAjax("POST", '@Url.Action("CheckLogin")', JSON.stringify(data), EditCallBack);
    }
    function EditCallBack(data) {
        if (data.result.IsSuccess) {
            window.location = '/Default/Index';
        } else {
            $("#message-result").css("color", "red");
            $("#message-result").text(data.result.Message);
        }
    }
</script>
</body>

</html>
